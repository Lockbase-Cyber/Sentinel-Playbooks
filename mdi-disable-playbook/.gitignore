# NOTE: infra/main.json IS committed (it's the Deploy-to-Azure button target).
# Keep it in sync with main.bicep by re-running `az bicep build` before commit.

# Local Azure CLI artifacts
.azure/
*.deployment.log

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
